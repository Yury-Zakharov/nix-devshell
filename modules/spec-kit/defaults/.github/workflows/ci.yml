name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run project tests
        run: |
          echo "TODO: replace with your actual test command"
          echo "Example: nix build .#test or cargo test or dotnet test"
          # Add your real CI command here once per project
